name = "bas-meta-report"
main = "src/index.ts"
compatibility_date = "2024-11-24"

# Cron Triggers - 매주 월요일 00:00 UTC (09:00 KST)
[triggers]
crons = ["0 0 * * MON"]

# Environment variables (non-secret)
[vars]
ENVIRONMENT = "production"
