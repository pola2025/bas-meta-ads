---
tags:
  - 시스템설계
  - 멀티클라이언트
  - Meta-API
  - Access-Token
  - 텔레그램
  - 주간리포트
  - 아카이브
  - bas_meta
  - 작업회고
date: 2025-11-21
project: bas_meta
status: 설계완료
type: 시스템설계
---

# 작업회고 - 멀티 클라이언트 지원 시스템 설계

## 📋 작업 개요

- **날짜**: 2025-11-21
- **프로젝트**: BAS Meta Ads Analytics
- **작업 범위**: 멀티 클라이언트 지원 구조 설계 및 가이드 작성
- **개발 환경**:
  - Supabase (PostgreSQL)
  - Meta Marketing API
  - Telegram Bot API
  - Railway (배포)

---

## 🎯 작업 목표

### 주요 목표
1. 다른 클라이언트 추가 방법 설계
2. Meta Access Token 관리 전략 수립
3. 주간 리포트 아카이브 기능 설계
4. System User Token 전환 가이드 작성

### 세부 요구사항
- 클라이언트별 데이터 완전 분리
- 텔레그램 채팅방 구분
- API 키 관리 자동화
- 리포트 히스토리 저장

---

## 🛠️ 주요 작업 내용

### 1. 멀티 클라이언트 지원 구조 설계

#### 현재 시스템 분석
**DB 구조**:
```sql
clients (클라이언트 마스터)
├── client_id (UUID, PK)
├── client_name
├── meta_ad_account_id
├── telegram_chat_id (NEW)
└── is_active

raw_data (원본 데이터)
├── id
├── client_id (FK) → 클라이언트별 분리
└── ...

weekly_summary (주간 요약)
├── id
├── client_id (FK) → 클라이언트별 분리
└── ...
```

**핵심 원칙**:
- ✅ 모든 테이블에 `client_id` 컬럼
- ✅ 쿼리 시 항상 `WHERE client_id = ?` 필터
- ✅ 데이터 섞임 방지 (완전 분리)

#### 설계 결과
**시나리오 1**: 같은 Meta 광고 계정
- clients 테이블에 새 행 추가
- 같은 Access Token 공유 가능
- 예: 비즈액터스쿨 A과정, B과정

**시나리오 2**: 다른 Meta 광고 계정
- clients 테이블에 새 행 추가
- Meta Ad Account ID 다름 (`act_987654321`)
- Access Token 클라이언트마다 별도 필요
- 개선 필요: `clients` 테이블에 `meta_access_token` 컬럼 추가

### 2. Meta Access Token 관리 전략

#### 현재 토큰 상태 확인
**파일**: `check-meta-token.js` 생성

**검증 결과**:
```
✅ Token is VALID
Type: USER (Long-Lived)
Expires At: 2026-02-17 (88일 남음)
Permissions: ads_management, ads_read, business_management
```

#### Token 종류 비교

| 종류 | 유효기간 | 자동연장 | 용도 | 권장 |
|------|----------|----------|------|------|
| Short-Lived | 1시간 | ❌ | 테스트 | ❌ |
| Long-Lived | 60일 | ❌ | 임시 운영 | △ |
| System User | 무제한 | ✅ | 프로덕션 | ✅ |

#### System User Token 전환 가이드
**파일**: `SYSTEM_USER_TOKEN_GUIDE.md` 작성

**핵심 단계**:
1. Meta Business Settings → System Users
2. System User 생성 ("BAS Meta Ads API")
3. 광고 계정 권한 부여 (Analyze 필수)
4. Token 생성 (Never expire 선택)
5. 환경 변수 업데이트

**소요 시간**: 5-10분
**난이도**: ⭐⭐☆☆☆

### 3. 주간 리포트 아카이브 기능 설계

#### 요구사항
- 텔레그램으로 발송된 주간 리포트를 대시보드에 저장
- 히스토리 조회 (아카이브)
- 주간/월간 리포트 구분
- AI 분석 결과 포함

#### DB 스키마 설계
**파일**: `sql/05_telegram_reports.sql` 생성

**테이블 구조**:
```sql
CREATE TABLE telegram_reports (
  id UUID PRIMARY KEY,
  client_id UUID REFERENCES clients(client_id),

  -- 리포트 기간
  week_start DATE NOT NULL,
  week_end DATE NOT NULL,
  report_type VARCHAR(20) DEFAULT 'weekly',

  -- 리포트 내용
  message_text TEXT NOT NULL,

  -- 핵심 성과 (빠른 조회용)
  total_spend DECIMAL(10,2),
  total_leads INTEGER,
  avg_cpl DECIMAL(10,2),
  avg_ctr DECIMAL(5,2),

  -- 전주 대비 변화
  spend_change DECIMAL(5,2),
  leads_change DECIMAL(5,2),
  cpl_change DECIMAL(5,2),

  -- AI 분석
  ai_insights TEXT,

  -- 발송 정보
  telegram_message_id INTEGER,
  sent_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**인덱스**:
- `idx_telegram_reports_client` (client_id)
- `idx_telegram_reports_date` (week_start DESC, week_end DESC)
- `idx_telegram_reports_unique` (client_id, week_start, week_end, report_type)

#### 대시보드 통합 계획
**위치**: https://dashboard-gamma-one-58.vercel.app

**새 탭 추가**: "주간 리포트 기록"
- 리포트 목록 (날짜별 정렬)
- 상세 보기 (메시지 전문 + AI 분석)
- 필터 (클라이언트, 기간, 타입)
- 검색 기능

### 4. 클라이언트 추가 스크립트

#### add-new-client.js 생성
**기능**:
- CLI로 새 클라이언트 추가
- clients 테이블에 자동 삽입
- 가이드 메시지 출력

**사용법**:
```bash
node add-new-client.js "새회사" "act_987654321"
node add-new-client.js "새회사" "act_987654321" "-1234567890"
```

**출력 정보**:
- Client ID (UUID)
- Meta Ad Account ID
- Telegram Chat ID
- 다음 단계 안내

---

## 📊 기대 효과

### 정량적 개선
- ✅ **무제한** 클라이언트 추가 가능
- ✅ **100%** 데이터 분리 (client_id 기반)
- ✅ **무제한** 토큰 유효기간 (System User Token)
- ✅ **아카이브** 기능으로 모든 리포트 히스토리 저장

### 정성적 개선
- ✅ 확장 가능한 시스템 구조
- ✅ 자동화된 클라이언트 관리
- ✅ 명확한 가이드 문서
- ✅ 리포트 히스토리 추적 가능

---

## 🔧 기술 스택

### Database
- **Supabase (PostgreSQL)**: 멀티 클라이언트 데이터 저장
- **Row Level Security**: 데이터 보안 (추가 예정)

### API
- **Meta Marketing API**: 광고 데이터 수집
- **Telegram Bot API**: 리포트 발송

### Infrastructure
- **Railway**: 자동화 워커 배포
- **Vercel**: 대시보드 호스팅

---

## 💡 배운 점

### 기술적 배움

1. **멀티테넌시 설계**
   - 모든 테이블에 tenant_id (client_id) 필수
   - 쿼리 레벨에서 필터링 강제
   - 인덱스 최적화 (client_id 포함)

2. **Meta Access Token 관리**
   - Short-Lived vs Long-Lived vs System User
   - 자동 갱신의 중요성
   - Business Manager 권한 구조

3. **DB 스키마 확장성**
   - 처음부터 멀티 클라이언트 고려
   - 외래 키 제약 조건
   - 유니크 인덱스로 중복 방지

### 프로세스 개선

1. **가이드 문서 선제 작성**
   - 구현 전 설계 문서화
   - Step-by-step 가이드
   - 문제 해결 섹션 포함

2. **자동화 스크립트**
   - CLI로 반복 작업 자동화
   - 명확한 사용법 제공
   - 에러 처리 및 안내

---

## 🐛 발생한 이슈 및 해결

### 이슈 1: Supabase에서 직접 SQL 실행 불가
**문제**:
- `exec_sql()` 함수가 존재하지 않음
- SQL 파일을 자동으로 실행할 수 없음

**원인**:
- Supabase는 보안상 동적 SQL 실행 제한
- RPC 함수를 미리 정의해야 함

**해결**:
- SQL 파일을 출력하여 Supabase Dashboard에서 수동 실행
- `create-telegram-reports-table.js` 스크립트로 SQL 표시

**교훈**:
- Supabase는 보안을 위해 동적 SQL 제한
- 마이그레이션 스크립트는 가이드 형태로 제공

### 이슈 2: Meta Access Token 만료일 혼동
**문제**:
- 사용자가 토큰 만료 여부 확인 요청
- 수동으로 확인하기 어려움

**해결**:
- `check-meta-token.js` 스크립트 작성
- Meta Graph API의 `debug_token` 엔드포인트 활용
- 만료일, 권한, 유효성 자동 확인

**결과**:
```bash
node check-meta-token.js
# → 토큰 상태, 만료일, 권한 즉시 확인
```

### 이슈 3: 클라이언트별 텔레그램 채팅방 구분
**문제**:
- 여러 클라이언트가 같은 채팅방으로 리포트 발송
- 혼동 발생 가능성

**해결**:
- `clients` 테이블에 `telegram_chat_id` 컬럼 추가 (설계)
- `telegram-cron.js`에서 클라이언트별 Chat ID 사용
- 기본값: 환경 변수 (TELEGRAM_ADMIN_CHAT_ID)

**구현 계획**:
```sql
ALTER TABLE clients ADD COLUMN telegram_chat_id TEXT;
```

```javascript
const chatId = client.telegram_chat_id || process.env.TELEGRAM_ADMIN_CHAT_ID;
```

---

## 📝 작업 파일 목록

### 생성된 파일

1. **sql/05_telegram_reports.sql**
   - 텔레그램 리포트 아카이브 테이블 스키마
   - 인덱스 및 RLS 정책
   - 사용 예시 포함

2. **add-new-client.js**
   - 새 클라이언트 추가 CLI 스크립트
   - 사용법 가이드 포함
   - 다음 단계 안내

3. **check-meta-token.js**
   - Meta Access Token 유효성 검증
   - 만료일 확인
   - 광고 계정 접근 테스트

4. **SYSTEM_USER_TOKEN_GUIDE.md**
   - System User Token 생성 가이드
   - 단계별 스크린샷 설명
   - 문제 해결 섹션

5. **create-telegram-reports-table.js**
   - Supabase 테이블 생성 가이드
   - SQL 출력 스크립트

### 변경 사항 요약

| 파일 | 변경 내용 | 상태 |
|------|----------|------|
| sql/05_telegram_reports.sql | 새 테이블 스키마 | 설계 완료 |
| add-new-client.js | 클라이언트 추가 스크립트 | 구현 완료 |
| check-meta-token.js | 토큰 검증 스크립트 | 구현 완료 |
| SYSTEM_USER_TOKEN_GUIDE.md | 토큰 전환 가이드 | 작성 완료 |

---

## 🎯 향후 작업 계획

### 단기 (즉시 - 1주)
- [ ] System User Token으로 전환 (선택)
- [ ] telegram_reports 테이블 생성 (Supabase Dashboard)
- [ ] telegram-cron.js에 리포트 저장 로직 추가
- [ ] 대시보드에 "주간 리포트 기록" 탭 추가

### 중기 (1-2주)
- [ ] clients 테이블에 `telegram_chat_id` 컬럼 추가
- [ ] telegram-cron.js 수정 (클라이언트별 Chat ID)
- [ ] clients 테이블에 `meta_access_token` 컬럼 추가 (선택)
- [ ] 클라이언트별 Access Token 관리

### 장기 (1개월)
- [ ] 월간 리포트 자동화
- [ ] 리포트 검색 기능
- [ ] 클라이언트 대시보드 (개별 로그인)
- [ ] Row Level Security 적용

---

## 🔍 회고 및 개선 사항

### 잘한 점

1. ✅ **체계적인 설계 선행**
   - 구현 전 시스템 구조 명확히 정의
   - 확장 가능한 DB 스키마 설계
   - 가이드 문서 선제 작성

2. ✅ **자동화 스크립트 제공**
   - 반복 작업 자동화
   - 명확한 사용법 제공
   - 에러 처리 포함

3. ✅ **다양한 시나리오 고려**
   - 같은 광고 계정 vs 다른 광고 계정
   - 텔레그램 채팅방 구분
   - 토큰 관리 전략

### 아쉬운 점

1. ❌ **실제 구현 미완료**
   - 설계와 가이드만 작성
   - telegram-cron.js 수정 필요
   - 대시보드 탭 추가 필요

2. ❌ **테스트 부족**
   - 실제 두 번째 클라이언트 추가 테스트 안 함
   - 멀티 클라이언트 환경 검증 필요

3. ❌ **토큰 자동 갱신 미구현**
   - System User Token 전환은 수동
   - 만료 알림 시스템 없음

### 개선 방안

1. **즉시 구현 완료**
   - telegram_reports 테이블 생성
   - telegram-cron.js 수정
   - 대시보드 탭 추가

2. **멀티 클라이언트 테스트**
   - 테스트용 두 번째 클라이언트 추가
   - 데이터 분리 검증
   - 텔레그램 발송 테스트

3. **모니터링 시스템**
   - 토큰 만료 알림 (30일 전)
   - 데이터 수집 실패 알림
   - Railway 배포 상태 모니터링

---

## 📚 참고 자료

### 공식 문서
- [Meta Marketing API](https://developers.facebook.com/docs/marketing-api/)
- [Meta System Users](https://developers.facebook.com/docs/marketing-api/system-users/)
- [Supabase Multi-tenancy](https://supabase.com/docs/guides/auth/row-level-security)

### 관련 작업 회고
- `작업회고-2025-11-21-보안사고-대응-SessionEnd-Hook-수정.md`
- `작업회고-2025-11-21-텔레그램자동리포트-광고일별추이-연동완료.md`

### 내부 문서
- `SYSTEM_USER_TOKEN_GUIDE.md` (이번 세션 작성)
- `sql/05_telegram_reports.sql` (이번 세션 작성)

---

## 🎉 결론

### 주요 성과

1. ✅ **멀티 클라이언트 지원 구조 설계 완료**
   - 확장 가능한 DB 스키마
   - 명확한 데이터 분리 원칙
   - 클라이언트 추가 프로세스 정립

2. ✅ **Meta Access Token 관리 전략 수립**
   - 토큰 종류 비교 분석
   - System User Token 전환 가이드
   - 자동 검증 스크립트 제공

3. ✅ **주간 리포트 아카이브 기능 설계**
   - DB 스키마 완성
   - 대시보드 통합 계획 수립
   - 히스토리 추적 가능

4. ✅ **완벽한 가이드 문서**
   - Step-by-step 설명
   - 문제 해결 포함
   - 즉시 적용 가능

### 다음 세션 작업

**우선순위 1**: 주간 리포트 아카이브 구현
1. telegram_reports 테이블 생성
2. telegram-cron.js 수정 (DB 저장)
3. 대시보드 탭 추가

**우선순위 2**: System User Token 전환
1. Meta Business Settings에서 생성
2. 환경 변수 업데이트
3. 검증 테스트

**우선순위 3**: 멀티 클라이언트 테스트
1. 테스트용 클라이언트 추가
2. 데이터 분리 검증
3. 텔레그램 발송 테스트

---

**작성일**: 2025-11-21
**버전**: 1.0
**상태**: 설계 완료, 구현 대기
**다음 작업**: 주간 리포트 아카이브 구현
