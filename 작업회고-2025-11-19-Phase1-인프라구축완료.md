---
tags:
  - 백엔드
  - Node.js
  - Supabase
  - PostgreSQL
  - Redis
  - BullMQ
  - Meta-API
  - OAuth
  - 인프라
  - 버그수정
  - 데이터수집
  - 토큰관리
  - BAS-Meta-Ads
  - 작업회고
date: 2025-11-19
project: BAS Meta Ads Analytics
status: 완료
type: 인프라구축
---

# BAS Meta Ads Phase 1 인프라 구축 완료 작업 회고

## 📋 작업 개요
- **날짜**: 2025-11-19
- **프로젝트**: BAS Meta Ads Analytics - 메타 광고 데이터 자동 수집 시스템
- **작업 범위**: Worker 디버깅 및 Phase 1 인프라 구축 완료
- **개발 환경**: Node.js, Supabase (PostgreSQL + Vault), Upstash Redis, Meta Graph API

## 🎯 작업 목표
1. Worker 실행 시 발생하는 토큰 파싱 에러 해결
2. 실제 Meta API 데이터 수집 1회 성공
3. raw_data 및 weekly_summary 테이블에 데이터 저장 검증
4. Phase 1 인프라 구축 완료 (95% → 100%)

## 🛠️ 주요 작업 내용

### 1. 문제 진단 및 분석
- 4개의 백그라운드 Worker 상태 확인
- 에러 로그 분석: `Meta API Error: Invalid OAuth access token - Cannot parse access token`
- Vault에 저장된 토큰과 .env 토큰 비교 분석

### 2. 토큰 파싱 이슈 해결 (핵심)
**발견한 문제**:
- Vault에 저장된 토큰에 **공백(whitespace)** 포함
- Vault 토큰: 228자, .env 토큰: 221자
- 공백으로 인해 Meta API 요청 실패

**해결 방법**:
- `lib/token-manager.js`의 `getAccessToken()` 및 `getRefreshToken()` 함수 수정
- Vault에서 토큰 가져온 후 `.replace(/\s/g, '')` 추가하여 모든 공백 제거

```javascript
// Before
return data.secret;

// After
return data.secret.replace(/\s/g, '');  // 공백 제거
```

**파일 위치**:
- `F:\bas_meta\lib\token-manager.js:199`
- `F:\bas_meta\lib\token-manager.js:223`

### 3. SQL 함수 에러 해결
**발견한 문제**:
- 토큰 이슈 해결 후 새로운 에러 발생
- `Failed to generate weekly summary: column reference "week_year" is ambiguous`
- PostgreSQL이 변수명 `week_year`를 컬럼과 혼동

**해결 방법**:
- 변수명에 `v_` 접두사 추가하여 명확하게 구분
- `week_year` → `v_week_year`
- `week_number` → `v_week_number`

**Migration 파일**:
- `supabase/migrations/20251118230117_fix_weekly_summary_ambiguous_column.sql`
- 한글 주석으로 인한 UTF-8 인코딩 에러 → 영어 주석으로 변경

### 4. 데이터 수집 성공 검증
**최종 테스트 프로세스**:
1. Redis 큐 클리어 (`clear-queue.js`)
2. Worker 재시작 (백그라운드 프로세스)
3. Producer 실행 (작업 큐에 추가)
4. Queue 상태 확인 → **Completed: 1, Failed: 0** ✅
5. Supabase 데이터 확인 (`check-collected-data.js`)

## 📊 기대 효과

### 정량적 지표
- ✅ 데이터 수집 성공률: **100%** (Failed: 0)
- ✅ raw_data 저장: **5개** 레코드
- ✅ weekly_summary 생성: **5개** 레코드
- ✅ Meta API 호출 성공: 토큰 파싱 에러 **0건**

### 정성적 개선
- 안정적인 OAuth 토큰 관리 (Vault 공백 문제 해결)
- 명확한 SQL 변수 네이밍으로 가독성 향상
- 엔드-투-엔드 데이터 수집 파이프라인 완성

## 🔧 기술 스택
- **Runtime**: Node.js v22.19.0
- **Queue**: BullMQ + Upstash Redis (Serverless)
- **Database**: Supabase PostgreSQL (RLS, Vault)
- **API**: Meta Graph API v22.0
- **Auth**: OAuth 2.0 (Long-lived tokens)
- **Libraries**:
  - `@supabase/supabase-js` - Supabase 클라이언트
  - `bullmq` - 큐 작업 처리
  - `ioredis` - Redis 클라이언트 (Upstash 최적화)
  - `node-fetch` - HTTP 요청

## 💡 배운 점

### 기술적 배움
1. **Supabase Vault의 동작 방식**
   - Vault에 저장할 때 예상치 못한 공백이 포함될 수 있음
   - 항상 `.trim()` 또는 `.replace(/\s/g, '')` 처리 필요
   - 보안 정보는 저장 시뿐만 아니라 조회 시에도 검증 필요

2. **PostgreSQL 변수 스코프**
   - DECLARE 블록의 변수명이 테이블 컬럼명과 같으면 ambiguous 에러 발생
   - 변수명에 접두사(`v_`, `p_` 등) 사용이 베스트 프랙티스
   - SELECT 절에서 변수를 사용할 때는 명확성이 중요

3. **BullMQ 디버깅 전략**
   - 백그라운드 프로세스는 로그 출력이 즉시 보이지 않음
   - `BashOutput` 도구로 비동기적으로 로그 확인 필요
   - Queue 상태 체크 스크립트 (`check-queue.js`) 필수

4. **Meta API 토큰 관리**
   - Access Token은 공백에 민감함
   - Long-lived token도 60일 후 만료 → 자동 갱신 로직 필수
   - Vault에서 가져온 토큰은 항상 sanitize 처리

### 프로세스 개선
1. **디버깅 순서 최적화**
   - .env 토큰으로 먼저 테스트 → Vault 토큰과 비교
   - 토큰 길이 차이 확인 → 공백 여부 검증
   - 단계별 검증으로 문제 범위 좁히기

2. **UTF-8 인코딩 주의**
   - SQL migration 파일에 한글 주석 사용 시 인코딩 에러 가능
   - 영어 주석 또는 UTF-8 BOM 없는 인코딩 사용 권장

## 🐛 발생한 이슈 및 해결

### 이슈 1: Meta API 토큰 파싱 에러
**문제**:
```
Meta API Error: Invalid OAuth access token - Cannot parse access token
```

**원인**:
- Vault에 저장된 토큰에 공백 포함 (228자 vs 221자)
- 저장 당시 문자열 처리 과정에서 공백 유입 추정

**해결**:
```javascript
// lib/token-manager.js
async getAccessToken(tokenId) {
  const { data, error } = await this.supabase.rpc('vault_read_secret', {
    secret_id: tokenId
  });

  // ⭐ 공백 제거 추가
  return data.secret.replace(/\s/g, '');
}
```

**재발 방지**:
- 토큰 저장 시에도 공백 제거 로직 추가 권장
- 토큰 검증 함수에서 길이 체크 추가

---

### 이슈 2: SQL 함수 ambiguous column 에러
**문제**:
```
Failed to generate weekly summary: column reference "week_year" is ambiguous
```

**원인**:
- `DECLARE week_year INTEGER;` 변수명이 테이블 컬럼 `week_year`와 동일
- PostgreSQL이 SELECT 절에서 어느 것을 참조해야 할지 혼동

**해결**:
```sql
-- Before
DECLARE
  week_year INTEGER;
  week_number INTEGER;

-- After
DECLARE
  v_week_year INTEGER;    -- v_ 접두사 추가
  v_week_number INTEGER;  -- v_ 접두사 추가
```

**파일**: `supabase/migrations/20251118230117_fix_weekly_summary_ambiguous_column.sql`

**재발 방지**:
- 모든 SQL 함수 변수에 `v_` 접두사 사용
- 파라미터는 `p_` 접두사 사용 (이미 적용됨)

---

### 이슈 3: UTF-8 인코딩 에러 (Migration)
**문제**:
```
ERROR: invalid byte sequence for encoding "UTF8": 0xfc
```

**원인**:
- Migration 파일에 한글 주석 포함
- Supabase CLI가 UTF-8 인코딩 처리 실패

**해결**:
- 모든 주석을 영어로 변경
- Migration 파일 재작성

**재발 방지**:
- SQL 파일은 항상 영어 주석 사용
- 또는 UTF-8 BOM 설정 확인

## 📝 작업 파일 목록

### 수정된 파일
1. **lib/token-manager.js**
   - `getAccessToken()`: 공백 제거 로직 추가 (line 199)
   - `getRefreshToken()`: 공백 제거 로직 추가 (line 223)

2. **supabase/migrations/20251118230117_fix_weekly_summary_ambiguous_column.sql**
   - `generate_weekly_summary()` 함수 변수명 변경
   - 한글 주석 → 영어 주석 변경

### 추가된 파일 (디버깅용)
1. **check-vault-token.js** - Vault 토큰과 .env 토큰 비교
2. **check-collected-data.js** - 수집된 데이터 검증
3. **clear-queue.js** - Redis 큐 초기화
4. **fix-weekly-summary.sql** - SQL 수정 임시 파일

## 🎯 향후 작업 계획

### 단기 (Phase 2 - 대시보드)
- [ ] Streamlit 대시보드 구축
  - 주간/월간 광고 성과 시각화
  - 캠페인별 ROI 분석
  - 리드 전환율 트래킹
- [ ] 데이터 수집 스케줄러 설정
  - pg_cron으로 매주 월요일 자동 실행
  - 또는 별도 스케줄러 (GitHub Actions, Railway Cron)
- [ ] 에러 알림 강화
  - Telegram 알림 개선
  - 실패 시 재시도 로직 추가

### 중기 (Phase 3 - 확장)
- [ ] 추가 클라이언트 등록
  - 다수 클라이언트 데이터 수집 테스트
  - 클라이언트별 설정 관리 UI
- [ ] 데이터 분석 기능
  - 자동 인사이트 생성 (AI 활용)
  - 이상치 탐지 및 알림
- [ ] 성능 최적화
  - 캐싱 전략 수립
  - 대용량 데이터 처리 최적화

### 장기 (Phase 4 - 자동화)
- [ ] 완전 자동화된 보고서 생성
  - 주간/월간 리포트 PDF 생성
  - 이메일 자동 발송
- [ ] 다채널 통합
  - Google Ads, Naver Ads 추가
  - 통합 대시보드 구축
- [ ] AI 기반 광고 최적화 제안
  - 성과 예측 모델
  - 예산 배분 최적화

## 🔍 회고 및 개선 사항

### 잘한 점 ✅
1. **체계적인 디버깅 접근**
   - 상태 확인 → 원인 분석 → 해결 → 검증 순서로 진행
   - 각 단계마다 로그와 검증 스크립트 작성

2. **재사용 가능한 유틸리티 작성**
   - `check-vault-token.js`, `check-collected-data.js` 등
   - 향후 디버깅 시 재활용 가능

3. **문서화 중심 작업**
   - 각 이슈마다 원인과 해결 방법 명확히 기록
   - NEXT_SESSION.md로 다음 세션 가이드 제공

4. **점진적 문제 해결**
   - 토큰 이슈 해결 후 SQL 에러 발견
   - 한 번에 모든 것을 고치려 하지 않고 단계별 접근

### 아쉬운 점 ⚠️
1. **Vault 저장 시 검증 부족**
   - 토큰 저장 시 공백 체크를 하지 않았음
   - 초기에 저장 전 sanitize 했다면 디버깅 시간 단축 가능

2. **SQL 변수 네이밍 컨벤션 미적용**
   - 초기부터 `v_` 접두사를 사용했다면 ambiguous 에러 방지 가능
   - 기존 코드 리뷰 시 발견했어야 함

3. **로그 출력 부족**
   - Worker에서 토큰 가져온 후 길이/형식 로그 출력 안 함
   - 디버깅에 더 많은 시간 소요

### 개선 방안 💡
1. **토큰 관리 강화**
   ```javascript
   // 토큰 저장 시
   async saveAccessToken(accessToken, clientName) {
     const sanitizedToken = accessToken.trim().replace(/\s/g, '');
     if (sanitizedToken.length !== accessToken.length) {
       console.warn(`Token had whitespace: ${accessToken.length} -> ${sanitizedToken.length}`);
     }
     // ... Vault 저장
   }

   // 토큰 조회 시
   async getAccessToken(tokenId) {
     const token = data.secret.replace(/\s/g, '');
     console.log(`Token length: ${token.length}`);
     return token;
   }
   ```

2. **SQL 코드 품질 체크**
   - SQL 함수 작성 시 변수명 컨벤션 체크리스트 작성
   - `v_` (변수), `p_` (파라미터), `t_` (임시 테이블) 규칙 문서화

3. **디버깅 로그 강화**
   ```javascript
   // Worker에 상세 로그 추가
   console.log('🔐 Token retrieved from Vault');
   console.log(`   Token ID: ${tokenId}`);
   console.log(`   Token length: ${accessToken.length}`);
   console.log(`   Token preview: ${accessToken.substring(0, 20)}...`);
   ```

4. **자동화된 검증**
   - Producer 실행 전 자동으로 토큰 유효성 검증
   - Migration 적용 후 함수 테스트 자동 실행

## 📚 참고 자료
- [Meta Graph API Documentation](https://developers.facebook.com/docs/graph-api)
- [Supabase Vault Documentation](https://supabase.com/docs/guides/database/vault)
- [BullMQ Documentation](https://docs.bullmq.io/)
- [PostgreSQL Variable Scoping](https://www.postgresql.org/docs/current/plpgsql-declarations.html)
- [Upstash Redis Serverless](https://docs.upstash.com/redis)

## 🎉 결론

**Phase 1 인프라 구축이 완전히 완료되었습니다!**

### 성과 요약
- ✅ Supabase Database (PostgreSQL + Vault) 완전 설정
- ✅ Upstash Redis 큐 시스템 작동
- ✅ Meta API 토큰 관리 및 자동 갱신 로직 검증
- ✅ Worker + Producer 데이터 수집 파이프라인 완성
- ✅ raw_data 및 weekly_summary 실제 데이터 저장 성공

### 최종 검증 데이터
**raw_data**: 5개 레코드
```
Date: 2025-11-15
Ad: 20251115_보험영업중이라면 꼭 보셔야 합니다
Impressions: 197, Clicks: 1, Spend: 11.34 USD
```

**weekly_summary**: 5개 레코드
```
Week: 2025-11-09 ~ 2025-11-15
Ad: 251101_삶이불안막막하다면
Impressions: 851, Clicks: 21, Spend: 72.87 USD, Leads: 2
```

이제 안정적인 데이터 수집 인프라 위에 대시보드와 분석 기능을 구축할 준비가 완료되었습니다! 🚀
